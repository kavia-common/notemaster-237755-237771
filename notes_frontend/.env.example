# PUBLIC_INTERFACE
# Frontend runtime configuration (Next.js static export compatible).
# Set these values in your real .env (do NOT commit secrets).

# Base URL of the FastAPI backend, e.g.:
# http://localhost:3001
# https://your-backend.example.com
NEXT_PUBLIC_NOTES_API_BASE_URL=
